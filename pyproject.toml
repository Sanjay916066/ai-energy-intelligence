[project]
name = "project-energy-intelligence"
version = "1.0.0"
description = "AI-Driven Energy Intelligence for Industrial Buildings — Case Study 1"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
